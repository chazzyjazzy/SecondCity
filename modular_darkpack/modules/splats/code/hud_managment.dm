// Called in both the inital hud init and when gaining a splat. To try and ensure its always added.
// Make sure to verify a hud element doesnt already exist as a result.
/datum/splat/proc/add_relevent_huds(datum/hud/hud_used)
	return

/datum/splat/proc/update_relevent_huds()
	return
